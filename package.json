{
  "name": "@treblahq/publishing-platform",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "lint": "eslint .",
    "scan:secrets": "node tooling/secret-scan.mjs",
    "pretest": "npm run build --workspace @treblahq/publishing-contracts && npm run build --workspace @treblahq/publishing-adapter-kit && npm run build --workspace @treblahq/publishing-adapter-test",
    "prevalidate": "npm run pretest && npm run types:worker && npm run build",
    "test": "NODE_NO_WARNINGS=1 vitest run --passWithNoTests",
    "types:worker": "wrangler types apps/worker/src/worker-configuration.d.ts --config apps/worker/wrangler.json",
    "typecheck": "tsc --noEmit",
    "validate": "npm run scan:secrets && npm run lint && npm run types:worker && npm run typecheck && npm test && npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^9.35.0",
    "@types/node": "^24.3.0",
    "eslint": "^9.35.0",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.42.0",
    "vitest": "^3.2.4",
    "wrangler": "4.129.0"
  }
}
